import {
  STORYBOOK,
  POST_API,
  DB,
  apiKey,
  authDomain,
  databaseURL,
  projectId,
  storageBucket,
  messagingSenderId,
  appId,
} from 'react-native-dotenv';

export const env = {
  STORYBOOK,
  POST_API,
  DB,
  apiKey,
  authDomain,
  databaseURL,
  projectId,
  storageBucket,
  messagingSenderId,
  appId,
};

export default env;
